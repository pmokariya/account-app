<%= form_with([@account, @account.transfers.build])  do |form| %>
  <% if transfer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(transfer.errors.count, "error") %> prohibited this transfer from being saved:</h2>

      <ul>
      <% transfer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.label :account_number %>
    <%= form.text_field :account_number, required: true, autofocus: true  %>
  </div>
  <div class="field">
    <%= form.label :account_pin %>
    <%= form.text_field :account_pin, required: true, autofocus: true  %>
  </div>
  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount, required: true, autofocus: true  %>
  </div>
 
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
